[project]
name = "product-control-center-backend"
version = "0.1.0"
description = "FastAPI backend for the sample CRUD application."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = [".."]
testpaths = ["tests"]
